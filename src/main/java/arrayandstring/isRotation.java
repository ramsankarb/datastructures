package arrayandstring;

/**
 * Created by Ramshankar on 9/20/16.
 * tocheck if the string1 is rotation of string2
 * like hello is rotation of llohe. there is issubstring function that can be leveraged but only once.
 */
public class isRotation {
    // tis is a trick question where we have to append the string twice and then send for substring method. like (s1s1,s2)
    // pure brilliance.
}
